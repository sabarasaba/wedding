import { validateGuest } from 'wedding/helpers/validate-guest';
import { module, test } from 'qunit';

module('Unit | Helper | validate guest');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validateGuest([42]);
  assert.ok(result);
});
